---
title: 一句话标题
description: 什么时候该走这条流程(写「什么情况下用」,不写「这是什么」)
requires: [tool_a, tool_b]
---

# 一句话标题

## 先分清用户说的是什么

同一句话往往对应几种不同的故障。列出可能的几种,以及用哪个工具、看哪个字段区分。

**这一步不能跳过** —— 后面的路径完全不同。

## 分支一:<情况>

1. 用 `tool_a` 看 <字段>。<取值 A> 与 <取值 B> 指向不同原因
2. 用 `tool_b` 核对 <字段>。对不上时的表现是 <现象>
3. 排除条件:<什么情况下这条分支不成立,该转到哪条>

## 分支二:<情况>

同上。

## 结论怎么给

- 该下判断就下,不用「疑似」
- 带上依据的字段名与取值
- 给出下一步:改哪个值、去哪个页面、还缺什么信息
- 排不出来时如实说明已排除哪几类、剩下哪几类平台数据判断不了
